# XiansAi Community Edition - Root Environment Configuration
# Copy this file to .env and the create-secrets.sh script will populate the passwords
# This file contains shared environment variables used across multiple services

# =============================================================================
# Shared PostgreSQL Credentials
# =============================================================================
# These credentials are shared by Temporal and Keycloak services
POSTGRES_USER=temporal
POSTGRES_PASSWORD=

# =============================================================================
# Docker Configuration
# =============================================================================
# Default configurations that can be overridden
VERSION=latest
ENV_POSTFIX=local
COMPOSE_PROJECT_NAME=xians-community-edition

# =============================================================================
# Optional: OpenAI API Key Override
# =============================================================================
# If set here, this will override the API key in server/.env.local
# Llm__ApiKey=
